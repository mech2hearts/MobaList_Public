{
  "name": "mobalist",
  "version": "1.0.0",
  "description": "Web app keeping track of users' mobile game information",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "cd server && node server.js",
    "start": "npm run client",
    "heroku-postbuild": "cd client && npm install && npm build"
  },
  "engines": {
    "node": "8.12.0",
    "npm": "6.4.1"
  },
  "author": "CK",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "jsonwebtoken": "^8.4.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
